[project]
name = "hardware-constrained-forge"
version = "0.1.0"
description = "Hardware-constrained kernels and operators"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "numpy",
    "triton",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
